import AppStatusScreen from './AppStatusScreen';

export default function FeatureReadinessLoading() {
  return (
    <AppStatusScreen
      title="Loading EcoTrack"
      message="Starting server and preparing the next page."
    />
  );
}
