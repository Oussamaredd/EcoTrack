apiVersion: v1
kind: Service
metadata:
  name: ecotrack-api
  namespace: ecotrack
  labels:
    app.kubernetes.io/name: ecotrack-api
spec:
  selector:
    app.kubernetes.io/name: ecotrack-api
  ports:
    - name: http
      port: 80
      targetPort: http
